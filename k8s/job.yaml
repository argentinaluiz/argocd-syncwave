apiVersion: batch/v1
kind: Job
metadata:
  name: myjob
spec:
  ttlSecondsAfterFinished: 100
  template:
    spec:
      containers:
      - name: myapp
        image: myapp
        command: ["node",  "index-for-deployment.js"]
      restartPolicy: Never
